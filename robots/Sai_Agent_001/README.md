# Sai_Agent_001

Preserved powered-clamp variant. [Guide](../../docs/SAI_AGENT_001.md).

For backward compatibility its frozen models remain in [`models/`](../../models),
with the shared policies in [`policies/`](../../policies). The catalog explicitly
maps this robot ID to those paths. No 002 geometry is substituted into 001.
